This paragraph contains enough words to verify that document formatting wraps prose at column twenty without losing text.

This paragraph should remain unchanged when only the first paragraph is selected for range formatting.

This paragraph is already
wrapped across several
short lines.

Auto wrapping should
